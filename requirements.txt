# This project currently uses only the Python standard library at runtime.
# Keep this file in the repository so dependencies can be pinned later and
# downloaded into .resources/packages for offline installation.
